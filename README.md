# coot0_9_to_1
transition from coot 0.9.x to coot 1.x
1. run coot 0.9.x (arm Linux) on macOS
2. use compatible layer to run old python codes for coot 0.9.x in coot 1.x on macOS
   you only need to change `print xx` to `print()` once you load compatible layer first.
